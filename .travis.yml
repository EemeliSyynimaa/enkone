compiler:
  - clang
  - gcc

language: c

script:
    - if [ "$CC" == "clang" ]; then make linux-clang && .build/linux64_clang/bin/helloworldDebug; fi
    - if [ "$CC" == "gcc" ]; then make linux-gcc && .build/linux64_clang/bin/helloworldDebug; fi
    - $CC --version
notifications:
    email: false